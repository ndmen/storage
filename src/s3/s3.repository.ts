export class S3Repository {
  async createOne(file): Promise<any> {
    // const createOne = await this.s3Model.create(file);
    // return createOne;
  }
  async findOne(id): Promise<any> {
    // const findOne = await this.s3Model.findById(id);
    // return findOne;
  }
}
